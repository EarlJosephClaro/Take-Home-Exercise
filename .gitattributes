# Normalize all text files to LF in the repository. This keeps the Makefile,
# Dockerfile and shell-invoked scripts working on Linux/macOS regardless of the
# committer's OS (CRLF in a Makefile breaks GNU make).
* text=auto eol=lf

# Binary types that must never be normalized.
*.png binary
*.jpg binary
*.ico binary
