# Port the HTTP server listens on
PORT=8787

# Base URL used to build the returned short_url. In production set this to your
# public origin, e.g. https://sho.rt
BASE_URL=http://localhost:8787

# Path to the SQLite database file. In Docker this defaults to /data/urls.db.
DB_PATH=./data/urls.db
